const InputHandler = require('../tested-input-handler');
const chai = require('chai');

const expect = chai.expect;
i
describe('inputHandler', () => {
    it('can be created', () => {
        const inputHandler = new InputHandler();
    });
});